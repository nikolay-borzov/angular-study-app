import { PatternInputDirective } from './pattern-input.directive';

describe('PatternInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatternInputDirective();
    expect(directive).toBeTruthy();
  });
});
